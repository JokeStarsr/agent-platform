# 开发进度审查清单（2026-09-04 夜间自动推进）

> 面向明早验收。本次为**自主推进**（用户就寝），完成内容以**验证 + 设计文档**为主，严格遵守"设计铁律"——W10 MCP 网关属重大架构决策，**仅产出设计文档，未写任何实现代码**，需你明早审批后才进入编码。

---

## 一、本次完成内容

### 1. 基线确认（验证性）
- 运行 `mvn test`：**109 个测试全绿，0 失败，BUILD SUCCESS**（含 ArchitectureTest 4/4）。
- 确认 W09 应用工厂消费点改造（commit `e1863b8`）后工作树干净、分支领先 origin 3 个 commit（未推送，网络原因）。

### 2. W09 收口状态审查
- AI 侧任务已全部落地：应用工厂（配置解析/装配/启停 API）、8 项配置 schema、客服双跑脚本 `scripts/eval/cs_dual_compare.py`、109 测试。
- 剩余为**人工验收项**（不在 AI 可自主范围）：新场景实测 ≤2 周、商旅 Golden Set 全量 badcase 审查、P2 阶段指标报告。

### 3. W10 MCP 网关核心 —— 设计文档（待检查，未实现）
按铁律先审后码，产出两份设计文档（**状态：待检查**）：

| 文档 | 路径 | 内容 |
|------|------|------|
| MCP 网关架构+接口 | `docs/design/architecture/20260904-mcp-gateway.md` | 双向网关模型（ADR）、入站 MCP Server（鉴权/审计/限流）、出站 Client 骨架、工具迁移零重写适配器、WireMock 契约测试、8 工具迁移对照表、接口契约、备选方案 |
| 授权表结构 | `docs/design/table/20260904-t-tool-grant.md` | t_tool_grant 租户级工具授权表（字段/索引/租户隔离/治理影响/建表 SQL） |

核心设计决策（需你确认）：
- **双向网关**：平台同时作 MCP Server（入站暴露本地工具）+ MCP Client（出站接外部工具，W11 基础）。
- **零重写迁移**：新增 `AgentToolAdapter` 桥接现有 `ToolEngineService`，幂等/校验/PAYMENT 403 全复用，不重写 W5-W8 已验证逻辑。
- **租户级授权**：MCP 调用无 appId → 授权判定从 per-app 白名单切到 per-tenant 的 `t_tool_grant`。
- **风险标注**：项目当前 servlet 栈 vs MCP starter 的 WebFlux 传输是否共存，列为实现时首个验证点（文档 §3.1/§9.1）。

### 3. 补充（夜间复核后增补，仍待检查）

复核 `20260904-mcp-gateway.md` 时发现原稿缺失排期 W10 第一项 AI 任务（Server 注册中心/心跳/上下线），已增补：
- **§3.5 幂等键在 MCP 层的强制**（READ 免键 / WRITE+PAYMENT 强制，缺失返回 -32602）
- **§3.6 Server 注册中心 / 健康检查 / 上下线事件**（`McpServerRegistry` + `GET /api/mcp/health` + up/down 审计事件）
- **§7.3 错误码映射表**（ToolEngine BizException → MCP JSON-RPC error code）
- **§7.4 契约测试分层**（单元 / WireMock 契约 / 端到端 integration）

---

## 二、待你决策事项

| # | 事项 | 建议 |
|---|------|------|
| 1 | **批准 W10 MCP 网关设计**（含 t_tool_grant 表） | 批准后我按 §11 六步实施（先验证 servlet/webflux 共存，再上适配器） |
| 2 | **W09 人工验收项** | 新场景实测 / Golden Set badcase 审查 / P2 指标报告需你参与 |
| 3 | **推送 3 个未推送 commit** | 需代理（此前 GitHub 连接被重置） |
| 4 | 设计文档研究依据来源 | 本次 MCP 技术细节来自模型训练知识 + 官方文档，WebFetch 被网络拦截；实现阶段需连 Spring AI 文档实测验证 API 名 |

---

## 三、后续迭代（待批准后）

1. W10 六步实施（依赖 → MCP 端点 → 适配器 → 鉴权+授权表 → 审计限流 → 出站+WireMock → 管理 API）
2. W11 工具市场（依赖出站网关）
3. P2 收口剩余：新场景实测、P2 指标报告

---

## 四、本次 git 变更

- 已提交 `d2fb4ef`：新增 `20260904-mcp-gateway.md`、`20260904-t-tool-grant.md`，更新 `docs/design/table/README.md` 与 REVIEW 清单。
- 复核增补（待提交）：`20260904-mcp-gateway.md` 补 §3.5/3.6/7.3/7.4，REVIEW 清单增补说明。

> 说明：按设计铁律，这两份"待检查"设计文档在获批前**不进入编码**。本次未写任何 MCP 实现代码，只完善了设计文档本身。
