# P2 阶段指标报告（2026-09-05）

> 依据：《开发排期》P2 阶段闸门与 W9 排期"出 P2 阶段指标报告"。本报告由 AI 侧从评测数据/git 历史/测试基线整理；**人工验收项**（新场景实测、badcase 审查）清单见 §5，需你参与。

---

## 1. 滑铁卢闸门对照（P2 阶段闸门四项）

| 闸门 | 目标 | 当前值 | 结论 |
|------|------|--------|------|
| 商旅任务端到端成功率 | ≥ 90% | **91.7%**（12 任务 11 成功，唯一失败为 zen 瞬时空输出，复跑即过） | ✅ |
| 写操作 100% 走 HITL + 幂等测试全绿 | 100% | book_order 挂起待审批；pay_order 硬 403；幂等三语义测试绿 | ✅ |
| 新场景接入 ≤ 2 周 | ≤ 2 周 | **未实测**（人工项，见 §5-1） | ⏳ |
| 客服场景迁移编排层指标不回退 | 不回退 | 双跑对比 6 条 handoff 翻转率 0%、引用集一致；评测 92/92 | ✅（冒烟） |

## 2. 关键指标汇总（W4-W9 各交付物实测值）

| 指标 | 数据点 |
|------|--------|
| Golden Set 评测（92 条） | 忠实度 **0.991** / Recall@5 **1.000** / 引用 **1.000** / 转人工率 **13.0%**（≤30%）/ 转人工正确率 **100%** |
| 首 Token 延迟（流式） | mean 868ms / **P95 1262ms**（≤2s 达标） |
| 商旅回归（12 种子任务） | 正确率 **91.7%**；违规拦截 5/5=100%（策略校验确定性拦截） |
| 工具选择准确率（W11 新增） | **93.9%**（49/50，≥90% P3 闸门达标） |
| 架构守护（ArchUnit） | 4/4 全绿，跨 W4-W12 持续保持 |
| 单元/契约测试基线 | 99 → 109 → 148 → 172 → **191**（每次交付不回退） |

## 3. 成本与模型路由

| 项 | 当前策略 | 说明 |
|----|----------|------|
| 本地模型路由 | sub2api → zen（优先）/ aliyun（兜底） | 10 分钟轮换；zen 免费通道为主，零 DeepSeek 消耗（deepseek-v2 已禁用） |
| CI 评测模型 | DeepSeek（经 env 覆盖） | GitHub 够不到本地网关，评测校准于 deepseek-chat |
| Token 计量 | P1 用 answerLen 代理 → **P2 已上线真实计量**（MeteredChatModel → metering.log，W11 收口） | 每次 LLM 调用记录 prompt/completion/total/latency |

## 4. W9 应用工厂关键结果

- 种子应用 2 个（cs_customer_service / tr_booking），启动幂等 upsert，ENABLED。
- 启停流实测：CREATED → start → ENABLED → suspend → SUSPENDED → re-start 全链路通过。
- 客服迁移：双跑对比（legacy vs app 链路）冒烟通过，指标不回退。
- 配置校验：缺项/工具未注册/PAYMENT 禁入/配额越界/handoff 阈值越界全部拦截。

## 5. 人工验收项清单（需你参与，AI 不能代办）

| # | 事项 | 说明 | 建议操作 |
|---|------|------|----------|
| 1 | **新场景实测 ≤ 2 周（G2 闸门实证）** | 排期 W9 明确"必须亲自做并计时" | 挑一个真实新场景（如 IT 运维问答），用应用工厂配置接入并记录耗时 |
| 2 | **商旅 Golden Set 全量 badcase 审查** | 12 种子任务的输出逐条审 | 跑 trip_runner.py 12 任务，逐条看答案/拦截是否符合预期 |
| 3 | **工具选择评测 3 条歧义 badcase** | 档位/改签/退款规则 3 条未命中 | 审 tool-selection.json 标注是任务表述问题还是工具描述问题 |
| 4 | 双模型互判校准 | runner 双调用取 min 已上线 | 跑一轮 golden_set_runner 看抖动是否下降，决定 JUDGE_MODELS 配置 |

## 6. 声明与出处

- 所有"当前值"来自 git 历史提交记录 + 测试基线 + 评测脚本实测（提交见 docs/CHANGELOG.md）。
- 本报告为 AI 侧交付，供你在验收时核对；不替代人工验收结论。