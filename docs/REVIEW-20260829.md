# 开发进度审查清单（2026-08-29）

> 面向明日审查：本轮已完成内容、待决策事项、遗留问题。配合 `docs/CHANGELOG.md` 使用。

---

## 一、本轮完成内容

### 1. sub2api 优先级调整（阿里云优先）
- **目标**：阿里云 > deepseek > zen-free
- **已改**：
  - `accounts.priority`：aliyun=1, deepseek=2, zen-free=10
  - `account_groups.priority`（group 1）：aliyun=1, deepseek=2, zen-free=3
  - aliyun `concurrency` 10 → 2（缓解并发限流）
- **验证**：优先级配置正确；但 aliyun 上游**持续 429**，sub2api 自动降级 deepseek 兜底

### 2. RAG 在线检索全链路打通
- **RagController**（`/api/rag/*`）：search / index / delete 三接口
- **RagService** 接入真实 LlmGateway（DeepSeek 生成）
- **实测通过**：灌入 3 份种子文档后，检索问答返回带引用【1】的正确答案
- 设计文档：`docs/design/api/20260829-rag-controller.md`

### 3. Golden Set 评测集 80 条
- 5 分类（退换货16/物流18/支付21/会员15/其他10）× 3 难度
- 每条含标准答案 + 来源文档定位
- 生成脚本：`scripts/eval/gen_golden_set.py`

### 4. 评测脚本重写
- 调用真实 RAG 接口 + 中文 bigram 相似度 + 真实 Recall@5
- 首次运行：**79/80 未达标**（见下方"遗留问题"）

### 5. 环境
- **pgvector**：走 daocloud 镜像源拉取成功，容器运行中
- **agent-platform**：8082 端口启动成功（8080 被 opencode2api 占用）
- **ZHIPU_API_KEY**：已配置，RAG embedding 用智谱

---

## 二、待你决策的事项

| # | 事项 | 现状 | 建议 |
|---|------|------|------|
| 1 | **aliyun 上游 429** | 阿里云 MaaS 持续返回 429，deepseek 兜底中 | 到阿里云控制台确认 token-plan 配额/QPS；降并发已试（无效，属账号级限流） |
| 2 | **评测 79/80 未达标** | 知识库仅 3 文档 8 切片，覆盖不足；bigram 近似过严 | 扩充知识库到 80 条问题全覆盖；忠实度改 LLM-as-judge |
| 3 | **端口 8080/8082** | opencode2api 占用 8080，agent-platform 用 8082 | 确认生产端口规划，避免长期用临时端口 |
| 4 | **git push 失败** | GitHub 连接被重置（网络） | 需配代理后推送（当前 3 个 commit 待推） |

---

## 三、遗留问题与后续迭代

1. **知识库扩充**：当前只有 3 份种子文档，需补齐 Golden Set 80 条对应的完整业务文档（尤其"其他/运维"类无文档）
2. **忠实度评测算法**：bigram 相似度对 LLM 重述性答案过严，P1 收口应换 LLM-as-judge
3. **CI 门禁**：评测脚本需接入 CI，指标低于基线阻断合并
4. **RAG SSE 流式**：当前 search 为同步响应，W3 客服界面需补流式端点

---

## 四、本轮 git 提交

| Commit | 内容 |
|--------|------|
| `3261d11` | RAG 在线检索 REST 接口 + 接入真实 LlmGateway |
| `e8da525` | Golden Set 评测集 80 条 + 评测脚本 + 种子知识库 |
| `7b899b8` | 忽略 Python 缓存产物 |
